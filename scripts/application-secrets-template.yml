# Secrets for production server
# Place in /opt/ship-o-hoi/application-secrets.yml

barentswatch:
  client-id: your-client-id
  client-secret: your-client-secret

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: your-email@gmail.com
    password: your-app-password

email:
  to: sindrehauglum@gmail.com, sha@lottstift.no

mapbox:
  access-token: pk.your-mapbox-public-token
