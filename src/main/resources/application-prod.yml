spring:
  config:
    import: file:/opt/ship-o-hoi/application-secrets.yml
  datasource:
    url: jdbc:sqlserver://localhost:1433;databaseName=ship_o_hoi;encrypt=false;trustServerCertificate=true
  mail:
    host: ${email.host}
    port: ${email.port}
    username: ${email.username}
    password: ${email.password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
email:
  to: sindrehauglum@gmail.com, sha@lottstift.no