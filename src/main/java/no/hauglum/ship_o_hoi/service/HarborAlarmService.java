package no.hauglum.ship_o_hoi.service;

import no.hauglum.ship_o_hoi.model.AISShip;
import org.springframework.stereotype.Service;

@Service
public class HarborAlarmService {

//    private final double harborLat;
//    private final double harborLon;
//    private final double alertRadiusNm; // nautiske mil
//
//    public void checkShip(AISShip ship) {
//        if (ship.destination() != null && ship.destination().toLowerCase().contains(myHarborName.toLowerCase())) {
//            double distance = GeoUtils.distanceNm(ship.latitude(), ship.longitude(), harborLat, harborLon);
//            if (distance <= alertRadiusNm && isHeadingTowards(ship, harborLat, harborLon)) {
//                triggerAlarm(ship, distance);
//            }
//        }
//    }
}
